# 早安吻语音与文本自定义与 AI 化

这份教程面向想制作数据包或发布示例包的玩家/整合包作者，说明三件事：

1. 早安吻静态文本怎么改。
2. 早安吻和残血救护的自定义语音包怎么做。
3. 早安吻的 AI 台词与 TTS 怎么接到 Touhou Little Maid 的 AI 站点。

> 本分支目标是 Minecraft Forge 1.20.1。示例数据包使用 `pack_format: 15`。

---

## 1. 目录结构

推荐的自定义数据包结构如下：

```text
MyTmaVoicePack/
├─ pack.mcmeta
└─ data/
   └─ touhou_maid_affection/
      ├─ morning_kiss/
      │  ├─ profile.json
      │  └─ voices/
      │     ├─ ciallo.ogg
      │     ├─ morning_test.ogg
      │     └─ onjdsk.ogg
      └─ emergency_rescue/
         ├─ profile.json
         └─ voices/
            └─ rescue_test.ogg
```

`pack.mcmeta` 示例：

```json
{
  "pack": {
    "pack_format": 15,
    "description": "Touhou Maid Affection custom voices"
  }
}
```

压缩发布时，zip 根目录必须直接包含 `pack.mcmeta` 和 `data/`。不要把整个 `MyTmaVoicePack` 外层目录再包一层放进 zip，否则 Minecraft 可能无法识别数据包。

---

## 2. 早安吻文本

早安吻静态文本放在：

```text
data/touhou_maid_affection/morning_kiss/profile.json
```

完整示例：

```json
{
  "kiss_sound_event": "touhou_maid_affection:touhou_maid_affection.kiss",
  "play_kiss_sound_with_voice": false,
  "dialogue_mode": "replace",
  "voice_mode": "append",
  "dialogue": {
    "morning": [
      "早安，{player}。今天也要让我陪在你身边哦。",
      "醒啦？{maid} 已经把今天第一份亲亲准备好了。",
      "太阳出来了，{player}，来一个会让运气变好的早安吻。"
    ],
    "evening": [
      "虽然已经不是早上了，但 {maid} 的亲亲依然有效。",
      "辛苦啦，{player}。这个吻就当作今天的存档奖励。"
    ],
    "general": [
      "啾。{player}，这下精神恢复了吗？",
      "{maid} 觉得，偶尔主动一点也没关系吧。"
    ]
  },
  "voice_files": [
    "ciallo.ogg",
    "morning_test.ogg",
    "onjdsk.ogg"
  ]
}
```

字段说明：

- `dialogue_mode`
  - `replace`：数据包台词覆盖模组内置台词。
  - `append`：数据包台词追加到模组内置台词。
- `voice_mode`
  - `append`：数据包语音追加到当前功能语音池。
  - `replace`：若存在数据包语音，则只保留数据包语音。
- `kiss_sound_event`
  - 早安吻复用的亲吻音效事件。音效响度不由数据包控制，而是跟随 `config/touhou_maid_affection-common.toml` 中的 `cooldown.kissSoundVolume`。
- `play_kiss_sound_with_voice`
  - `true` 时，播放语音时同时播放亲吻音效。
- `dialogue`
  - 三个分类：`morning`、`evening`、`general`。
  - 文本支持 `{maid}`、`{player}` 占位符。
  - 元素有两种写法：纯字符串（旧写法，视为「未标记」，任何显示语种都可选中），或 `{"text": "...", "language": "zh_cn"}` 带语种标签。
- `voice_files`
  - 相对 `morning_kiss/voices/` 的 OGG 文件名列表。
  - 元素有两种写法：纯字符串（旧写法，视为「未标记」），或 `{"file": "x.ogg", "language": "ja_jp", "text": "可选字幕", "text_language": "zh_cn"}`。
  - `text` / `text_language` 让这条语音自带一句配对字幕，播放时直接显示它而不是随机抽台词。

> 两种写法的语种标签与筛选规则见下一节「[显示语种与配音语种](#3-显示语种与配音语种)」。

---

## 3. 显示语种与配音语种

TMA 1.7.4.0 起，**字幕/文本语种**与**配音语种**可以分别配置。两个配置项都在 `config/touhou_maid_affection-common.toml` 的 `morningKissBehavior` 段：

```toml
[morningKissBehavior]
displayLanguage = "auto"   # 字幕 / 文本语种
voiceLanguage   = "auto"   # 配音语种
```

- 取值：
  - `auto`（默认）：**文本语种跟随游戏语言**（内置台词按客户端语言渲染、未标记的数据包台词通配、AI 台词沿用女仆的 TLM 聊天语言），**配音语种跟随女仆的 TLM AI 语言设置**（数据包语音不按语言过滤、AI 合成沿用女仆的 TLM TTS 语言）。这就是 1.7.3.0 的行为。
  - 显式 locale：`zh_cn`、`ja_jp`、`en_us`、`zh_tw`…… 大小写与 `-`/`_` 均可（`zh-CN` 等价 `zh_cn`），表示**固定**该语种。
  - 旧关键字 `tlm`、`inherit`、`default` 继续接受，语义等价 `auto`（面板与状态页显示归一化后的 `auto`）。
- 只有这两个语种配置，没有 AI 专用覆盖层：1.7.5.0 删除了 `aiDialogueLanguage` / `aiDialogueVoiceLanguage`，它们原先控制的 AI 台词/配音语言现在同样由 `displayLanguage` / `voiceLanguage` 决定。
- 说明：
  - **内置台词**是 i18n key，由**客户端**按自己的语言渲染，服务端无法指定渲染语种，因此内置台词始终跟随客户端语言。`dialogue_mode=append` 时，内置台词被当作「语言 = 客户端语言」的条目参与同一套筛选：若显式 `displayLanguage` 与客户端语言不一致，内置条目会被排除，避免语言混杂。
  - **TLM 音包**（`maid_sound.json` 等）没有任何语言元数据，天然是单语种，**不参与语种筛选**；`voiceLanguage` 只影响数据包语音。
  - 数据包 `voice_files[].text` 是可选字幕：仅当该语音被选中播放时才使用它，且若带 `text_language` 且与目标显示语种不一致则不用它，退回随机台词。

数据包两种写法可以混用（向后兼容）：

```json
{
  "dialogue": {
    "morning": [
      "旧写法：未标记，任何显示语种都能选中。",
      {"text": "中文早安台词。", "language": "zh_cn"},
      {"text": "日本語のセリフ。", "language": "ja_jp"}
    ]
  },
  "voice_files": [
    "legacy.ogg",
    {"file": "jp_morning.ogg", "language": "ja_jp", "text": "配对的中文字幕。", "text_language": "zh_cn"}
  ]
}
```

选择规则（`displayLanguage` / `voiceLanguage` 为显式 locale 时）：

1. 优先取 `language` 与目标语种匹配的条目；
2. 没有匹配时取「未标记」（旧写法）条目；
3. 再没有时退回全部条目（保证旧包不被打破）。

**中文字幕 + 日语配音示例**：

```toml
[morningKissBehavior]
displayLanguage = "zh_cn"
voiceLanguage   = "ja_jp"
```

配合上面的数据包：台词优先选中 `language = "zh_cn"` 的中文条目，语音优先选中 `language = "ja_jp"` 的日语文件；若该日语文件带 `text`（且 `text_language = "zh_cn"`），这次播放就直接显示它的配对中文字幕。

---

## 4. 残血救护语音

残血救护的语音池放在：

```text
data/touhou_maid_affection/emergency_rescue/profile.json
```

示例：

```json
{
  "version": 1,
  "voice_mode": "append",
  "voice_files": [
    "rescue_test.ogg"
  ],
  "sound_event": "minecraft:entity.player.levelup"
}
```

字段说明：

- `voice_mode`
  - `append`：把数据包语音追加到残血救护候选池。
  - `replace`：若存在数据包语音，则只保留数据包语音。
- `voice_files`
  - 相对 `emergency_rescue/voices/` 的 OGG 文件名列表。
- `sound_event`
  - 没有可播放语音时的兜底音效。

---

## 5. 语音文件规则

- 语音文件必须是 `.ogg`。
- 路径必须写相对文件名或相对子路径，不能写绝对路径。
- 路径不能包含 `..` 或反斜杠 `\`。
- `voice_files` 里写了文件名，就必须把对应 OGG 放进同功能的 `voices/` 目录。
- 文件过大时解析器会跳过并在日志里警告，发布前建议在游戏里实际试听一次。

在羁绊页的早安吻语音池和残血救护语音池二级页中，可以选中候选项后按“试听语音”按键，或在列表项上右键试听。数据包语音会向服务端请求预览字节；TLM 音包和内置音效优先走客户端本地播放。

---

## 6. 生成 AI 台词

早安吻的 AI 台词和 TTS 不写进数据包 JSON，而是放在模组配置里：

```text
config/touhou_maid_affection-common.toml
```

相关配置位于 `morningKissBehavior` 段，主要包括：

### 6.1 基础开关

- `aiDialogueEnabled`：是否启用早安吻 AI 台词集成（默认 `false`）。
- `aiDialoguePregenerateEnabled`：是否在非触发时段提前生成缓存（默认 `true`）。关闭后仅在使用时通过即时回退生成。
- `aiDialogueImmediateFallbackEnabled`：缓存未命中时是否现场请求 TLM Chat（默认 `false`）。开启可确保每次触发都有 AI 台词，但会引入实时延迟。
- `aiDialogueTtsEnabled`：预生成台词时是否尝试生成 TTS 语音（默认 `true`）。仅当女仆配置了远程 TTS 站点时生效，系统 TTS 和非 OGG 响应会被跳过。
- `voiceVolume`：早安吻 TLM 音包、数据包和 AI/TTS 语音的音量倍率（默认 `1.0`，范围 `0.0`–`1.0`，只做衰减：`0.0` 静音、`1.0` 保持原有响度，不再放大）。亲吻音效仍使用 `cooldown.kissSoundVolume`。

### 6.2 语言控制

- `displayLanguage`（文本语种）与 `voiceLanguage`（配音语种）：同时作用于数据包与 AI 链路，默认都是 `auto`，详见第 3 节。
  - `auto` / `tlm` / `inherit` / `default`：跟随 Touhou Little Maid 本体语言设置。文本预生成跟随女仆聊天语言（Chat Language），生成式语音缓存会优先跟随 TLM 原生"语音合成"语言按钮（TTS Language），确保送入 TTS 的文本语种和请求语种一致。
  - `zh_cn`、`en_us`、`ja_jp` 等具体 locale：统一覆盖该链路的语言，prompt 中会追加对应语言的生成指令。

语言解析链：

- 文本生成语言：`displayLanguage`（显式 locale 时）→ TLM Chat Language → TLM TTS Language
- TTS 语音文本语言：`voiceLanguage`（显式 locale 时）→ TLM TTS Language → TLM Chat Language
- TTS 请求语言：与 TTS 语音文本语言一致

### 6.3 提示词

- `aiDialoguePrompt`：早安吻台词提示词模板（默认含 `{maid}`、`{player}`、`{pool}`、`{time}` 占位符）。运行时会替换占位符，并根据 `displayLanguage` 追加语言生成指令。

### 6.4 扫描策略

- `aiDialogueScanIntervalTicks`：服务端扫描间隔（默认 `1200` tick = 60 秒）。仅在非允许时间窗口内扫描，避免与触发时段竞争。
- `aiDialogueScanDistance`：扫描玩家附近女仆的距离（默认 `16` 格）。

### 6.5 缓存容量与消费策略

- `aiDialogueCacheTargetPerPool`：每名女仆、每个时段池的缓存硬上限（默认 `4`，范围 `1-8`）。三个时段池（`morning`、`evening`、`general`）合计最多 `4 × 3 = 12` 条，不因语种分组而扩容。缓存添加和 TTS 回调入池时都会检查此上限。
- `aiDialogueCacheConsumeOnUse`：是否在早安吻触发时消耗缓存条目（默认 `false`）：
  - `false`（推荐）：缓存条目被复用而不消耗，播放后不会自动补池。服务端按当前解析出的显示/配音语种判定满度：改了 `displayLanguage` / `voiceLanguage` 后无需手动清理，系统会自动按新语种重新预热（旧语种条目保留、不再被选中，可用 `/tma morning_kiss clear_ai_cache` 手动清理）。此模式可大幅节省 LLM/TTS token。
  - `true`：每次使用后消耗一条缓存，服务端会在下次扫描时补充至目标数量。适合希望每次触发都有新鲜台词的使用场景。

### 6.6 调试

- `aiDialogueVerboseLog`：是否输出 AI 预生成调试日志（默认 `false`）。

### 6.7 缓存持久化

生成的 AI 台词和 TTS 语音不仅保存在运行内存中，还会持久化到世界目录下：

```text
<world>/generated_morning_kiss/<maidUuid>/
├─ morning/
│  ├─ 001.json
│  ├─ 001.ogg
│  ├─ 002.json
│  └─ ...
├─ evening/
│  └─ ...
└─ general/
   └─ ...
```

该组合会先生成中文显示台词，再批量翻译出逐行对应的日文 `tts_text`，最后用日文文本生成语音。这里的中日组合只是示例：两个配置接受任意 locale code，可组合英文+韩文、日文+中文、法文+德文等语言。翻译失败时只保留显示文字，不播放语义不匹配的语音。

- **保存时机**：每次有新条目成功加入缓存时自动触发持久化；服务器关闭时也会完整保存。
- **加载时机**：服务器启动时自动从世界目录加载已有缓存。
- **文件格式**：每个 JSON 文件包含 `text`（显示文本）、`tts_text`（送入 TTS 的文本）、`voice_file`（关联语音文件名）、`text_language`、`voice_language`、`maid_name`。语音文件为 OGG 或 MP3。
- **可编辑**：玩家或整合包作者可在停服后直接编辑 JSON 文本或替换 OGG 文件，下次进入世界时自动生效。
- **与内存缓存同步**：持久化目录是运行时缓存的磁盘镜像。任何一次清理操作都会同时更新磁盘镜像，保持两者一致。

### 6.8 管理命令

`/tma morning_kiss` 命令组提供完整的 AI 缓存管理能力，需要 op 权限（level 2）的操作会额外标注：

| 命令 | 说明 | 权限 |
| --- | --- | --- |
| `/tma morning_kiss` | 查看早安吻功能总状态（开关、AI/TTS 状态、最终生效语种、缓存策略） | 无 |
| `/tma morning_kiss status` | 同上 | 无 |
| `/tma morning_kiss ai on/off` | 打开/关闭 AI 台词生成。关闭时会同步关闭预生成并清空缓存 | op |
| `/tma morning_kiss tts on/off` | 打开/关闭生成式 TTS | op |
| `/tma morning_kiss cache` | 按女仆、时段池、文本语种、语音语种展示生成缓存明细，以及进行中的 LLM/TTS 请求 | 无 |
| `/tma morning_kiss clear_ai_cache` | 清空所有女仆的 AI 缓存（含持久化） | op |
| `/tma morning_kiss clear_ai_cache maid <uuid>` | 清空指定女仆的全部时段池缓存 | op |
| `/tma morning_kiss clear_ai_cache pool <uuid> <pool>` | 清空指定女仆的指定时段池（morning/evening/general） | op |
| `/tma morning_kiss clear_ai_cache entry <uuid> <pool> <index>` | 删除指定位置的某条缓存条目 | op |
| `/tma morning_kiss clear_ai_cache voice <uuid> <pool> <index>` | 仅清除某条缓存的语音数据，保留文本 | op |

### 6.9 工作方式

1. 服务端在非触发时段（不在 `allowedTimeRanges` 内时）每隔 `aiDialogueScanIntervalTicks` 扫描附近已解锁早安吻的女仆。
2. 若女仆已有足够缓存（达到 `aiDialogueCacheTargetPerPool`），跳过；否则发起 LLM 请求预生成台词。
3. LLM 返回后，若 TTS 可用且 `aiDialogueTtsEnabled` 开启，同时发起 TTS 请求预生成语音。
4. 全部结果通过 `addIfCurrent` 加入缓存——每次加入前会校验缓存版本号，确保清理操作不会与过期写入冲突。
5. 触发早安吻时根据 `aiDialogueCacheConsumeOnUse` 决定"随机取出一条并移除"（`consume=true`）或"随机取出一条但不移除"（`consume=false`，默认）。
6. 缓存未命中时，若 `aiDialogueImmediateFallbackEnabled=true` 则现场请求 AI；否则回退到静态台词、数据包语音或原有播放链路。
7. AI 失败只影响增强体验，不应阻断普通早安吻。

### 6.10 缓存条目信息

每条缓存包含以下字段：

- `text`：显示给玩家的台词文本
- `tts_text`：实际送入 TTS 引擎的文本（默认为 text；当后台预生成会生成远程 TTS 时，会跟随 TLM TTS 语言按钮）
- `voiceFileName`：语音文件名（空表示纯文本缓存）
- `voiceData`：语音二进制数据（OGG 或 MP3）
- `textLanguage`：文本语言代码
- `voiceLanguage`：语音请求语言代码
- `maidName`：女仆显示名称

文本过滤规则：

- 去除列表标记（`- `、`* `、`• `、`1. `、`1) ` 等）
- 去除外层引号（中英文双引号、单引号）
- 按显示宽度截断（半角=1，全角=2），超过 96 丢弃
- 语音数据仅接受 OGG（以 `OggS` 开头）和 MP3（以 `ID3` 或 FF FB/E0 开头），单条上限 2MB

---

## 7. 接入 AI 站点

早安吻的台词生成与 TTS 语音完全使用车万女仆自己的 AI 站点：

1. 在 Touhou Little Maid 的 AI 设置中配置聊天（LLM）与 TTS 站点，填写 API key、服务地址与模型/音色。
2. 回到游戏即可生效；TMA 不再注册自己的站点类型，也不保存你的密钥，一切都由 TLM 自己管理（`config/touhou_little_maid/sites/*.json`）。

音色由 TLM 站点配置决定（voice/model 选择）；GPT-SoVITS 站点另有其原生的 prompt 字段。TMA 不提供全局音色提示词。

---

## 8. 打包发布建议

本仓库自带的示例包位于：

```text
examples/TMA-Custom-Voice-Pack
```

发布时建议进入该目录，选中 `pack.mcmeta` 与 `data/` 后压缩成 zip。zip 根目录应长这样：

```text
TMA-Custom-Voice-Pack.zip
├─ pack.mcmeta
└─ data/
```

建议保留：

- 清晰的 `pack.mcmeta` 描述。
- 至少一条早安吻语音和一条残血救护语音。
- `morning / evening / general` 三个早安吻台词分类。
- 与 `voice_files` 完全对应的 OGG 文件列表。

不要把 AI 开关写回数据包里，运行时配置应该留在 TOML。

---

## 9. 常见错误

- 压缩后 zip 根目录不是 `pack.mcmeta` 和 `data/`。
- 1.20.1 数据包仍使用 1.21 系的 `pack_format`。
- 只写 `voice_files`，却忘了把 OGG 放进对应的 `voices/` 目录。
- 把 AI 生成开关塞进数据包 JSON。
- 文件名使用反斜杠、绝对路径或 `..`。
- 用 MP3/WAV 直接放进数据包语音池。
- 把 `replace` 和 `append` 的语义写反。

如果你只是想做一个能直接发给玩家的示例包，照着 [examples/TMA-Custom-Voice-Pack](examples/TMA-Custom-Voice-Pack) 改最省心。

---

## 10. 游戏内设置面板（1.7.5.0+）

羁绊页顶部左侧新增「设置」按钮，打开的是**全局设置**面板（1.7.5.0 起为**独立窗口**，与当前女仆无关；关闭后返回羁绊页）。面板分三个区。

### 能在面板里改的项

| 面板项 | 对应 toml 配置 | 是否需要 OP |
|---|---|---|
| 早安吻 | `morningKissBehavior.enabled` | 是 |
| 女仆主动早安吻 | `morningKissBehavior.autoEnabled` | 是 |
| AI 台词生成 | `morningKissBehavior.aiDialogueEnabled` | 是 |
| AI 语音合成 | `morningKissBehavior.aiDialogueTtsEnabled` | 是 |
| 残血救护 | `emergencyRescueBehavior.enabled` | 是 |
| 随机礼物 | `bondCosts.randomGiftBehavior.enabled` | 是 |
| 少女祈祷 Buff | `buff.enabled` | 是 |
| 文本语种 | `morningKissBehavior.displayLanguage` | 是 |
| 配音语种 | `morningKissBehavior.voiceLanguage` | 是 |
| 亲吻音效音量 | `cooldown.kissSoundVolume` | 否（纯客户端） |
| 早安吻语音音量 | `morningKissBehavior.voiceVolume` | 否（纯客户端） |
| 残血救护音量 | `emergencyRescueBehavior.volume` | 否（纯客户端） |
| 语音试听音量 | `voicePreview.volume` | 否（纯客户端） |

### 仍然需要改 toml 的项

- 时间窗 `allowedTimeRanges`、好感度门槛 `requiredFavorabilityLevel`、距离/超时、亲吻次数与间隔。
- AI 缓存策略：`aiDialoguePregenerateEnabled`、`aiDialogueImmediateFallbackEnabled`、`aiDialogueScanIntervalTicks`、`aiDialogueCacheConsumeOnUse`、`aiDialogueVerboseLog`。
- AI 提示词 `aiDialoguePrompt`。
- 救护阈值、每日次数、视觉旋转偏移，膝枕距离/姿态，粒子与 FOV 等。
- 所有数据包内容（台词、语音文件）依然只由数据包控制。

### 语种取值规则

面板的下拉框提供 `auto` + 常见 locale（`zh_cn` / `en_us` / `ja_jp` / `zh_tw` / `ko_kr`），当前值不在列表里时会自动补上。校验规则：

- 允许 `auto` / `tlm` / `inherit` / `default`（大小写不敏感，统一按小写保存）。
- 允许小写 locale：`zh_cn`、`ja_jp`、`en_us`、`pt_br`……（2–8 个小写字母 + 可选 `_` 子标签），长度 ≤ 32。
- **大写 locale（如 `ZH_CN`）与 `zh-CN` 会被拒绝**。toml 里的旧值仍按旧逻辑解析（`-` 会转成 `_`、大小写不敏感），但如果你的 toml 里写的是 `zh-CN` 这类形式，面板会把它显示为当前值，却无法再次选中——请改成 `zh_cn`。

### 多人服务器上的权限语义

- `ModConfig` 是 `COMMON` 配置，**不会自动同步**。服务端权威项（开关与语种）现在通过 `TmaSettingsRequestPayload` / `TmaSettingsStatePayload` 读写：客户端只发请求，服务端校验并写入**服务端自己的**配置文件，然后把完整状态回推给客户端。
- **只有权限等级 ≥ 2 的 OP 可以修改**；普通玩家能打开面板、能看到当前值，但开关与语种按钮不可点击，tooltip 与面板底部会提示「只读 / 需要管理员权限」。
- 每次成功修改都会在服务端日志打印审计行：`[TMA Settings] player=<玩家名> key=<逻辑键> old=<旧值> new=<新值>`。越权请求与非法请求（未知 key、非法布尔、非法语种、超长值）打印 WARN；**任一条不合法就整包拒绝**，不会部分生效。
- 音量是纯客户端设置，直接写入玩家自己的 `config/touhou_maid_affection-common.toml`，不影响其他玩家；单人存档里因为客户端与服务端共用同一份配置，所以看起来是「全局」的。
- 四项音量滑块的取值范围是 `0.0`–`1.0`（步进 `0.05`），**只做衰减**：`0.0` 静音、`1.0` 保持原有响度，不提供放大。需要更大音量请调整 Minecraft 或系统音量。1.7.5.0 之前写在 toml 里的 `>1.0` 旧值会在配置加载时被配置系统纠正为 `1.0`。
