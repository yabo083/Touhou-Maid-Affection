# 早安吻语音与文本自定义与 AI 化

这份教程说明三件事：

1. 早安吻静态文本怎么改。
2. 早安吻和残血救护的自定义语音包怎么做。
3. 早安吻的 AI 台词与 TTS 怎么接到 TLM AI 站点。

---

## 1. 目录结构

推荐的自定义数据包结构如下：

```text
MyTmaVoicePack/
├─ pack.mcmeta
└─ data/
   └─ touhou_maid_affection/
      ├─ morning_kiss/
      │  ├─ profile.json
      │  └─ voices/
      │     ├─ ciallo.ogg
      │     ├─ morning_test.ogg
      │     └─ onjdsk.ogg
      └─ emergency_rescue/
         ├─ profile.json
         └─ voices/
            └─ rescue_test.ogg
```

`pack.mcmeta` 示例：

```json
{
  "pack": {
    "pack_format": 48,
    "description": "Touhou Maid Affection custom voices"
  }
}
```

---

## 2. 早安吻文本

早安吻静态文本放在：

```text
data/touhou_maid_affection/morning_kiss/profile.json
```

示例：

```json
{
  "kiss_sound_event": "touhou_maid_affection:touhou_maid_affection.kiss",
  "play_kiss_sound_with_voice": false,
  "dialogue_mode": "replace",
  "voice_mode": "append",
  "dialogue": {
    "morning": [
      "早安，{player}。今天也要让我陪在你身边哦。",
      "醒啦？{maid} 已经把今天第一份亲亲准备好了。",
      "太阳出来了，{player}，来一个会让运气变好的早安吻。",
      "今天的第一句喜欢你，由 {maid} 认真送达。"
    ],
    "evening": [
      "虽然已经不是早上了，但 {maid} 的亲亲依然有效。",
      "辛苦啦，{player}。这个吻就当作今天的存档奖励。",
      "夜色很好，所以 {maid} 决定偷偷把晚安吻提前给你。"
    ],
    "general": [
      "啾。{player}，这下精神恢复了吗？",
      "{maid} 觉得，偶尔主动一点也没关系吧。",
      "靠近一点，{player}。今天也请多多指教。",
      "这是数据包测试用的随机早安吻台词，{maid} 正在营业中。"
    ]
  },
  "voice_files": ["ciallo.ogg", "onjdsk.ogg"]
}
```

字段说明：

- `dialogue_mode`
  - `replace`：数据包台词覆盖模组内置台词。
  - `append`：数据包台词追加到模组内置台词。
- `voice_mode`
  - `append`：数据包语音追加到当前功能语音池。
  - `replace`：若存在数据包语音，则只保留数据包语音。
- `kiss_sound_event`
  - 早安吻复用的亲吻音效事件。音效响度不由数据包控制，而是跟随 `config/touhou_maid_affection-common.toml` 中的 `cooldown.kissSoundVolume`。
- `play_kiss_sound_with_voice`
  - `true` 时，播放语音时同时播放亲吻音效。
- `dialogue`
  - 三个分类：`morning`、`evening`、`general`。
  - 文本支持 `{maid}`、`{player}` 占位符。
  - 元素有两种写法：纯字符串（旧写法，视为「未标记」，任何显示语种都可选中），或 `{"text": "...", "language": "zh_cn"}` 带语种标签。
- `voice_files`
  - 相对 `morning_kiss/voices/` 的 OGG 文件名列表。
  - 元素有两种写法：纯字符串（旧写法，视为「未标记」），或 `{"file": "x.ogg", "language": "ja_jp", "text": "可选字幕", "text_language": "zh_cn"}`。
  - `text` / `text_language` 让这条语音自带一句配对字幕，播放时直接显示它而不是随机抽台词。

> 两种写法的语种标签与筛选规则见下一节「[显示语种与配音语种](#3-显示语种与配音语种)」。

---

## 3. 显示语种与配音语种

TMA 1.7.4.0 起，**字幕/文本语种**与**配音语种**可以分别配置。两个配置项都在 `config/touhou_maid_affection-common.toml` 的 `morningKissBehavior` 段：

```toml
[morningKissBehavior]
displayLanguage = "auto"   # 字幕 / 文本语种
voiceLanguage   = "auto"   # 配音语种
```

- 取值：
  - `auto`（默认）：**保持 1.7.3.0 现状**，不对数据包台词/语音做任何语种筛选。
  - 显式 locale：`zh_cn`、`ja_jp`、`en_us`、`zh_tw`…… 大小写与 `-`/`_` 均可（`zh-CN` 等价 `zh_cn`）。`tlm`、`auto`、`default` 一律视为「未指定」。
- 优先级（越具体越优先）：
  1. AI 专用配置 `aiDialogueLanguage` / `aiDialogueVoiceLanguage` 是显式 locale；
  2. 全局 `displayLanguage` / `voiceLanguage` 是显式 locale；
  3. 旧语义（`tlm` / `inherit` / `auto`，跟随女仆的 TLM 语言）。
- 说明：
  - **内置台词**是 i18n key，由**客户端**按自己的语言渲染，服务端无法指定渲染语种，因此内置台词始终跟随客户端语言。`dialogue_mode=append` 时，内置台词被当作「语言 = 客户端语言」的条目参与同一套筛选：若显式 `displayLanguage` 与客户端语言不一致，内置条目会被排除，避免语言混杂。
  - **TLM 音包**（`maid_sound.json` 等）没有任何语言元数据，天然是单语种，**不参与语种筛选**；`voiceLanguage` 只影响数据包语音。
  - 数据包 `voice_files[].text` 是可选字幕：仅当该语音被选中播放时才使用它，且若带 `text_language` 且与目标显示语种不一致则不用它，退回随机台词。

数据包两种写法可以混用（向后兼容）：

```json
{
  "dialogue": {
    "morning": [
      "旧写法：未标记，任何显示语种都能选中。",
      {"text": "中文早安台词。", "language": "zh_cn"},
      {"text": "日本語のセリフ。", "language": "ja_jp"}
    ]
  },
  "voice_files": [
    "legacy.ogg",
    {"file": "jp_morning.ogg", "language": "ja_jp", "text": "配对的中文字幕。", "text_language": "zh_cn"}
  ]
}
```

选择规则（`displayLanguage` / `voiceLanguage` 为显式 locale 时）：

1. 优先取 `language` 与目标语种匹配的条目；
2. 没有匹配时取「未标记」（旧写法）条目；
3. 再没有时退回全部条目（保证旧包不被打破）。

**中文字幕 + 日语配音示例**：

```toml
[morningKissBehavior]
displayLanguage = "zh_cn"
voiceLanguage   = "ja_jp"
```

配合上面的数据包：台词优先选中 `language = "zh_cn"` 的中文条目，语音优先选中 `language = "ja_jp"` 的日语文件；若该日语文件带 `text`（且 `text_language = "zh_cn"`），这次播放就直接显示它的配对中文字幕。

---

## 4. 残血救护语音

残血救护的语音池放在：

```text
data/touhou_maid_affection/emergency_rescue/profile.json
```

示例：

```json
{
  "version": 1,
  "voice_mode": "append",
  "voice_files": ["rescue_test.ogg"],
  "sound_event": "minecraft:entity.player.levelup"
}
```

说明：

- `voice_files` 相对 `emergency_rescue/voices/`。
- `sound_event` 是没有可播放语音时的兜底音效。

---

## 5. 生成 AI 台词

早安吻的 AI 台词和 TTS 不再写进数据包 JSON，而是放在模组配置里：

```text
config/touhou_maid_affection-common.toml
```

常见配置项在 `morningKissBehavior` 段中，主要包括：

- 是否启用 AI 台词预生成。
- 预生成扫描间隔和距离。
- 触发时是否优先使用缓存结果，以及是否消耗缓存条目。
- TTS 是否启用。
- 失败时是否回退到静态台词或数据包语音。
- `voiceVolume`：早安吻 TLM 音包、数据包和 AI/TTS 语音音量；亲吻音效仍使用 `cooldown.kissSoundVolume`。
- `aiDialogueLanguage`：屏幕上显示的 AI 台词语言。
- `aiDialogueVoiceLanguage`：发送给 TTS 的配音文本语言；不同于显示语言时会先翻译并保持逐行配对。
- `displayLanguage` / `voiceLanguage`：全局字幕/配音语种（1.7.4.0 新增），同时作用于数据包与 AI 链路。AI 专用配置是显式 locale 时优先于它们，详见第 3 节。
- 缓存容量。

工作方式是：

1. 服务端在非触发时段扫描附近已解锁早安吻的女仆。
2. 若已配置 TLM LLM/TTS 站点，则预先生成台词和语音。
3. 触发早安吻时优先命中缓存。
4. 缓存没有命中时，回退到静态台词、数据包语音或原有播放链路。

v1.7.2.2 起新增或改进了以下行为：

- **缓存磁盘持久化**：AI 缓存不再仅是内存数据。服务器启动时自动从 `world/generated_morning_kiss/{maid_uuid}/{pool}/` 恢复，关闭时自动保存，重启不丢失。
- **`aiDialogueCacheConsumeOnUse`**（默认 `false`）：控制缓存条目的消耗模式。`false` 时 peek 复用条目，`true` 时 poll 消耗条目，用于平衡 LLM/TTS Token 成本与体验多样性。
- **`aiDialogueLanguage` 默认值改为 `tlm`**：未显式配置时将跟随各女仆的 TLM 聊天语言和 TTS 语言偏好，而非全局固定中文。语言解析区分文本生成（优先使用聊天语言）和 TTS 语音生成（优先使用 TTS 语言）。
- **显示与配音语言分离**：`aiDialogueVoiceLanguage=inherit` 默认保持旧配置兼容；显示语言为显式值时继承它，显示语言跟随 TLM 时则跟随女仆的 TTS 语言。也可显式设置独立语言：

```toml
[morningKissBehavior]
aiDialogueLanguage = "zh_cn"
aiDialogueVoiceLanguage = "ja_jp"
```

该组合会先生成中文显示台词，再批量翻译出逐行对应的日文 `tts_text`，最后用日文文本生成语音。这里的中日组合只是示例：两个配置接受任意 locale code，可组合英文+韩文、日文+中文、法文+德文等语言。翻译失败时只保留显示文字，不播放语义不匹配的语音。

跨语言模式要求先命中预生成缓存。如果缓存暂时为空，系统会跳过旧的即时 AI/TTS 回退并显示静态台词，同时抑制不匹配的随机语音；缓存生成完成后才播放成对的中文台词与日文配音。
- **管理命令**：除 `clear_ai_cache` 外，新增：
  - `/tma morning_kiss status`：查看 AI/TTS 开关状态与缓存总数概览。
  - `/tma morning_kiss cache`：按女仆和语言池查看详细统计。
  - `/tma morning_kiss ai on|off`：实时开关 AI 台词生成。
  - `/tma morning_kiss tts on|off`：实时开关 TTS 语音生成。
  - `clear_ai_cache` 支持女仆级、池级、条目级和语音级清理粒度。

---

## 6. 接入 MiMo

如果你想在 TLM 的 AI 设置页里使用 MiMo：

1. 启用模组的 MiMo 适配器。
2. 在 Touhou Little Maid 的 AI 设置中新增 MiMo Chat / MiMo TTS 站点。
3. 填好 API key 和服务地址。
4. 回到羁绊页，点击 MiMo 入口按钮即可跳转到 TLM AI 设置页。

TMA 只提供站点类型和默认值，不保存你的密钥。

---

## 7. 打包发布建议

发布示例包时，建议直接压缩 `MyTmaVoicePack/` 目录：

- 保留清晰的 `pack.mcmeta`。
- 至少放一条早安吻语音和一条残血救护语音。
- 早安吻台词最好保留 `morning / evening / general` 三个分类。
- 不要把 AI 开关写回数据包里，运行时配置应该留在 TOML。

---

## 8. 常见错误

- 只写 `voice_files`，却忘了把 OGG 放进对应的 `voices/` 目录。
- 把 AI 生成开关塞进数据包 JSON。
- 文件名使用反斜杠或绝对路径。
- 用非 OGG 音频直接发布。
- 把 `replace` 和 `append` 的语义写反。

如果你只是想做一个能直接发给玩家的示例包，照着 [examples/TMA-Custom-Voice-Pack](examples/TMA-Custom-Voice-Pack) 改最省心。
