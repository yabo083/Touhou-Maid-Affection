## 互动语音数据包配置教程

早安吻和残血救护的语音池已经拆开，每个功能各管各的：

```text
data/touhou_maid_affection/morning_kiss/voices/*.ogg

data/touhou_maid_affection/emergency_rescue/voices/*.ogg
```

### 1. 数据包结构

```text
MyTmaVoicePack/
├─ pack.mcmeta
└─ data/
   └─ touhou_maid_affection/
      ├─ morning_kiss/
      │  ├─ profile.json
      │  └─ voices/
      │     ├─ morning_01.ogg
      │     └─ morning_alt.ogg
      └─ emergency_rescue/
         ├─ profile.json
         └─ voices/
            ├─ rescue_01.ogg
            └─ rescue_alt.ogg
```

`pack.mcmeta`：

```json
{
  "pack": {
    "pack_format": 48,
    "description": "Touhou Maid Affection custom voices"
  }
}
```

### 2. 早安吻语音池

```json
{
  "version": 1,
  "kiss_sound_event": "touhou_maid_affection:touhou_maid_affection.kiss",
  "play_kiss_sound_with_voice": true,
  "dialogue_mode": "replace",
  "voice_mode": "append",
  "voice_files": ["morning_01.ogg", "morning_alt.ogg"],
  "dialogue": {
    "morning": [],
    "evening": [],
    "general": []
  },
  "ai_dialogue": {
    "enabled": false,
    "language": "zh_cn",
    "prompt": ""
  }
}
```

路径相对 `data/touhou_maid_affection/morning_kiss/voices/`。

### 3. 残血救护语音池

```json
{
  "version": 1,
  "voice_mode": "append",
  "voice_files": ["rescue_01.ogg", "rescue_alt.ogg"],
  "sound_event": "minecraft:entity.player.levelup"
}
```

路径相对 `data/touhou_maid_affection/emergency_rescue/voices/`。

### 4. voice_mode

- `append`：默认推荐。数据包语音追加到该功能的可选语音池里。
- `replace`：数据包语音存在时，覆盖该功能的默认/TLM 语音池。

残血救护不会再通过数据包按女仆指定具体播放哪条语音；玩家应在游戏内 GUI 为每个女仆选择随机全部、随机分组或指定语音项。

### 5. 早安吻台词、AI 与亲吻音效

早安吻静态台词和 AI 提示词仍保留在：

```text
data/touhou_maid_affection/morning_kiss/profile.json
```

这里可以继续配置 `dialogue_mode`、`dialogue`、`kiss_sound_event`、`play_kiss_sound_with_voice`、`voice_mode`、`voice_files` 与 `ai_dialogue`。

`play_kiss_sound_with_voice` 控制早安吻播放数据包/TLM 语音时，是否同时播放原生亲吻音效。默认 `true`，保留“说话 + 亲吻背景音”的效果；设为 `false` 时，有语音候选时只播放语音。

### 6. 文件要求

- 文件必须是 `.ogg`。
- 单个文件最大 2 MiB。
- 路径不能包含 `..`、反斜杠或绝对路径。

修改后进游戏执行 `/reload` 生效。
